{
  "name": "starcraft-llm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:python": "python3 -m unittest discover -s tests -v",
    "test:all": "npm test && npm run test:python"
  }
}
